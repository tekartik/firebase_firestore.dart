name: firebase_auth_repo_support
description: Firebase repo (tools)
version: 0.2.0
publish_to: none

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dev_dependencies:
  path:
  pub_semver:
  test:
  dev_test:
  pedantic:


